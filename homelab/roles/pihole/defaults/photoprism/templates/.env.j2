# PhotoPrism Environment Configuration
PHOTOPRISM_ADMIN_USER={{ photoprism_admin_user }}
PHOTOPRISM_ADMIN_PASSWORD={{ photoprism_admin_password }}
PHOTOPRISM_SITE_URL={{ photoprism_site_url }}
PHOTOPRISM_SITE_TITLE={{ photoprism_site_title }}
PHOTOPRISM_SITE_CAPTION={{ photoprism_site_caption }}
PHOTOPRISM_SITE_DESCRIPTION={{ photoprism_site_description }}
PHOTOPRISM_SITE_AUTHOR={{ photoprism_site_author }}

# Performance Settings
PHOTOPRISM_WORKERS={{ photoprism_workers }}
PHOTOPRISM_READONLY={{ photoprism_readonly | lower }}

# Database Configuration
PHOTOPRISM_DATABASE_DRIVER={{ photoprism_db_driver }}
PHOTOPRISM_DATABASE_SERVER={{ photoprism_db_host }}:{{ photoprism_db_port }}
PHOTOPRISM_DATABASE_NAME={{ photoprism_db_name }}
PHOTOPRISM_DATABASE_USER={{ photoprism_db_user }}
PHOTOPRISM_DATABASE_PASSWORD={{ photoprism_db_password }}
PHOTOPRISM_DATABASE_ROOT_PASSWORD={{ photoprism_db_root_password }}

# Storage Paths
PHOTOPRISM_ORIGINALS_PATH={{ photoprism_originals_path }}
PHOTOPRISM_STORAGE_PATH={{ photoprism_storage_path }}
PHOTOPRISM_IMPORT_PATH={{ photoprism_import_path }}
PHOTOPRISM_DIR={{ photoprism_dir }}

# Feature Flags (AI features disabled)
PHOTOPRISM_DISABLE_FACES={{ photoprism_disable_faces | lower }}
PHOTOPRISM_DISABLE_CLASSIFICATION={{ photoprism_disable_classification | lower }}
PHOTOPRISM_DISABLE_TENSORFLOW={{ photoprism_disable_tensorflow | lower }}
PHOTOPRISM_DISABLE_TGA={{ photoprism_disable_tga | lower }}
PHOTOPRISM_DISABLE_BACKUPS={{ photoprism_disable_backups | lower }}
PHOTOPRISM_DISABLE_WEBDAV={{ photoprism_disable_webdav | lower }}
PHOTOPRISM_DISABLE_SETTINGS={{ photoprism_disable_settings | lower }}
PHOTOPRISM_DISABLE_PLACES={{ photoprism_disable_places | lower }}
PHOTOPRISM_DISABLE_EXIFTOOL={{ photoprism_disable_exiftool | lower }}
PHOTOPRISM_DISABLE_FFMPEG={{ photoprism_disable_ffmpeg | lower }}
PHOTOPRISM_DISABLE_DARKTABLE={{ photoprism_disable_darktable | lower }}
PHOTOPRISM_DISABLE_RAWTHERAPEE={{ photoprism_disable_rawtherapee | lower }}
PHOTOPRISM_DISABLE_SIPS={{ photoprism_disable_sips | lower }}
PHOTOPRISM_DISABLE_HEIFCONVERT={{ photoprism_disable_heifconvert | lower }}

# Upload Settings
PHOTOPRISM_UPLOAD_NSFW={{ photoprism_upload_nsfw | lower }}
PHOTOPRISM_DETECT_NSFW={{ photoprism_detect_nsfw | lower }}
PHOTOPRISM_EXPERIMENTAL={{ photoprism_experimental | lower }}

# Logging
PHOTOPRISM_LOG_LEVEL={{ photoprism_log_level }}
PHOTOPRISM_DEBUG={{ photoprism_debug | lower }}
